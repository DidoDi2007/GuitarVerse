@model List<GuitarVerse.Models.User>
@{
    ViewData["Title"] = "Manage Users";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

    // Взимаме ID на текущия админ
    var currentUserId = Context.Session.GetInt32("UserID");
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold">User Management</h2>
    <span class="badge bg-secondary fs-6">Total Users: @Model.Count</span>
</div>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<div class="card border-0 shadow-sm">
    <div class="card-body p-0">
        <table class="admin-table">
            <thead>
                <!-- 6 КОЛОНИ В ХЕДЪРА -->
                <tr>
                    <th>ID</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Verified</th>
                    <th>Joined Date</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                <!-- ВАЖНО: Цикълът трябва да е ТУК, преди <tr> -->
                @foreach (var user in Model)
                {
                    <tr>
                        <!-- 1. ID -->
                        <td>#@user.UserID</td>

                        <!-- 2. EMAIL -->
                        <td>
                            <span class="fw-bold">@user.Email</span>
                            @if (user.UserID == currentUserId)
                            {
                                <span class="badge bg-primary ms-2">YOU</span>
                            }
                        </td>

                        <!-- 3. ROLE (Цветните баджове) -->
                        <td>
                            @if (user.Role == "superadmin")
                            {
                                <span class="badge bg-warning text-dark"><i class="fa-solid fa-crown"></i> SUPER ADMIN</span>
                            }
                            else if (user.Role == "admin")
                            {
                                <span class="badge bg-danger">ADMIN</span>
                            }
                            else
                            {
                                <span class="badge bg-info text-dark">Customer</span>
                            }
                        </td>

                        <!-- 4. VERIFIED -->
                        <td>
                            @if (user.IsEmailVerified)
                            {
                                <span class="text-success"><i class="fa-solid fa-check-circle"></i> Yes</span>
                            }
                            else
                            {
                                <span class="text-muted"><i class="fa-solid fa-circle-xmark"></i> No</span>
                            }
                        </td>

                        <!-- 5. DATE -->
                        <td>@user.CreatedAt.ToShortDateString()</td>

                        <!-- 6. ACTIONS -->
                        <td class="text-end">
                            <!-- Ако е Супер Админ - не го пипаме -->
                            @if (user.Role == "superadmin")
                            {
                                <span class="text-muted small"><i class="fa-solid fa-shield"></i> Protected</span>
                            }
                            else if (user.UserID != currentUserId) // Ако не си ти самият
                            {
                                <!-- Бутон за смяна на роля -->
                                <form asp-action="ToggleUserRole" method="post" style="display:inline;">
                                    <input type="hidden" name="id" value="@user.UserID" />
                                    @if (user.Role == "customer")
                                    {
                                        <button type="submit" class="btn btn-sm btn-outline-success" title="Promote to Admin" onclick="return confirm('Make this user an Admin?');">
                                            <i class="fa-solid fa-arrow-up"></i>
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="submit" class="btn btn-sm btn-outline-warning" title="Demote to Customer" onclick="return confirm('Remove Admin rights?');">
                                            <i class="fa-solid fa-arrow-down"></i>
                                        </button>
                                    }
                                </form>

                                <!-- Бутон за триене -->
                                <form asp-action="DeleteUser" method="post" style="display:inline;">
                                    <input type="hidden" name="id" value="@user.UserID" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger ms-1" title="Delete User" onclick="return confirm('Are you sure? This will delete all their data.');">
                                        <i class="fa-solid fa-trash"></i>
                                    </button>
                                </form>
                            }
                            else
                            {
                                <span class="text-muted small">Current Account</span>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>